#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

int main(){
    vector<int> nums;
    int a;
    //Open File
    ifstream infile;
    infile.open("input.txt");
    infile >> a;
    //Loop to Make Vertex
    for(int i = 0; i < a; i++){
        
    }
}